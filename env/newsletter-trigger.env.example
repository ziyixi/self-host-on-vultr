# Copy to a NEW private env/newsletter-trigger.env, mode 600.
# Only these two service capabilities belong here. Never include provider keys,
# ingestion tokens, Codex auth, or the newsletter.env file wholesale.
# These must match the values in env/newsletter.env and differ from each other.
NEWSLETTER_EDITOR_TOKEN=
NEWSLETTER_SEND_TOKEN=
